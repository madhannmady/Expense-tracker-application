import { ReactNode } from "react";
import { TabPaneBaseType } from "../TabPane/interface";
export declare type TabType = TabPaneBaseType & {
    content?: ReactNode | (() => ReactNode);
};
export interface TabsBaseType {
    type?: 'line' | 'text' | 'miniCard';
    activeTab?: any;
    tabList: TabType[];
    activeTabChange?: ActiveTabChangeType;
}
export declare type TabsHandle = {
    activeTab: any;
};
export declare type TabsPropsType = TabsBaseType;
export declare type ActiveTabChangeType = (key: any, tabPaneProps: TabPaneBaseType) => void;
