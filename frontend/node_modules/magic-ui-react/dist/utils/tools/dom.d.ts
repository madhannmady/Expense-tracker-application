/**
 * className获取Element对象 有则直接获取，没有则创建
 * @param className 类名
 * @param config 配置项
 * @returns
 */
export declare const getElement: (className: string, config?: {
    attributes?: {
        qualifiedName: string;
        value: string;
    }[];
}) => [
    Element,
    {
        destory: () => void;
    }
];
/**
 * 主题切换
 * @param url 主题css地址
 */
export declare function loadThemeStyles(url: string): void;
