import { MutableRefObject } from "react";
import { FormBaseType, FormItemBaseType } from "../Form/interface";
export declare type SearchColumnType = {
    formItem: Pick<FormItemBaseType, 'name' | 'label'>;
    type: 'text' | 'number';
};
export declare type SearchActionType = {
    search: () => void;
    reset: () => void;
};
export interface SearchBaseType {
    columns: SearchColumnType[];
    actionRef?: MutableRefObject<SearchActionType | undefined>;
    className?: string;
    formProps?: FormBaseType;
    search?: (values: Record<string, any>) => void;
    reset?: () => void;
}
export declare type SearchPropsType = SearchBaseType;
